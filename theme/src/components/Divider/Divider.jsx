/** @jsx jsx */
import { jsx } from 'theme-ui';

export const Divider = ({ props }) => {
  return (
    <div
      style={{
        height: '70px',
      }}
      sx={{ borderLeft: 'dotted' }}
    ></div>
  );
};
